export const environment = {
  production: true,
  // Empty string means relative URLs — Netlify proxies /api/* to BACKEND_URL
  // Set BACKEND_URL in the Netlify site's environment variables.
  backendUrl: '',
};
